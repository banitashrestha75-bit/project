# Streamlit secrets file
# This file is kept empty because API keys are loaded from the local .env file.
# Keeping this file here prevents Streamlit and third-party libraries (like LlamaIndex)
# from throwing StreamlitSecretNotFoundError when accessing secrets.
